{
    var a : Integer

    function add2(n : Integer, sum : Integer) : Integer {
        % if n = 0 then {
        %     return with sum
        % }
        % else {
        %     sum := sum + 2
            % var i : Integer
            % i := add2(n-1, sum)
            return with add2(n - 1, sum)
        % }
    }

    a := add2(5, 0)

}

% stack: UNDEFINED, UNDEFINED, 
